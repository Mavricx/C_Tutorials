#include <stdio.h>
int main()
{
    int a, b, sum;
    printf("Enter first number:");
    scanf("%d", &a);
    printf("Enter second Number:");
    scanf("%d", &b);
    sum = a + b;
    printf("sum of both numbers will be: %d", sum);
    return 0;
}