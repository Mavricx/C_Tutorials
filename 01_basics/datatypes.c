#include <stdio.h>
int main()
{
    // int age = 49;
    // float pi = 3.14;
    // char hagtag = '#';
    // printf("age is %d\n", age);
    // printf("hagtag is %c\n", hagtag);
    // printf("pi is %f\n", pi);
    int rating;
    printf("please enter your rating on this c course");
    scanf("%d", &rating);
    printf("rating is %d\n", rating);
    return 0;
}