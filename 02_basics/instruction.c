#include <stdio.h>
#include <math.h>

int main()
{
    int a = 3, b = 2;
    // int power = pow(a, b);
    // printf("power of %d raised to %d is %d\n", a, b, power);
    printf("Remainder is %d", 4 % 3);
}
